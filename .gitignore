node_modules/
release/
build/png/
*.log
boot.err
boot.out
npm-debug.log*
# セッション/設定の退避ファイル（パスワードを含むためコミット禁止）
waterm-sessions*.json
waterm-settings*.json
waterm-snippets*.json
*.bak
*.corrupt
# ローカル開発用ヘルパー（マシン依存のキャッシュ参照）
stamp-electron-version.ps1
# 自己署名のコード署名証明書（秘密鍵はコミット禁止）
build/*.pfx
build/*.cer
# 署名ビルドのヘルパー（証明書パスワードを含むためコミットしない）
build/dist-signed.ps1
